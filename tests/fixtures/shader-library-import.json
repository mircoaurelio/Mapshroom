{
  "name": "Library Import Check",
  "code": "// NAME: Library Import Check\nuniform float speed; // @min 0.0 @max 2.0 @default 1.0\nvec4 processColor(sampler2D tex, vec2 uv, float time, vec2 resolution) { return vec4(uv.x, uv.y, 0.5 + 0.5 * sin(time * speed), 1.0); }",
  "uniformValues": { "speed": 0.5 }
}
